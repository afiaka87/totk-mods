add_executable(fidelity_tests test_policy.cpp test_aesthetics.cpp test_options.cpp)
target_compile_features(fidelity_tests PRIVATE cxx_std_20)
target_include_directories(fidelity_tests PRIVATE "${DOCTEST_DIR}" ../../src/surface ../../src/pure stubs)
add_test(NAME surface_scan COMMAND fidelity_tests)

foreach(profile IN ITEMS constrained)
    add_executable(survey_${profile}_options ../test_main.cpp test_options.cpp)
    target_compile_features(survey_${profile}_options PRIVATE cxx_std_20)
    target_include_directories(survey_${profile}_options PRIVATE "${DOCTEST_DIR}" ../../src/pure)
    target_compile_definitions(survey_${profile}_options PRIVATE SURVEY_CONSTRAINED=1)
    add_test(NAME survey_${profile}_options COMMAND survey_${profile}_options)
endforeach()
if(MINGW)
    target_link_options(fidelity_tests PRIVATE -static)
    target_link_options(survey_constrained_options PRIVATE -static)
endif()
